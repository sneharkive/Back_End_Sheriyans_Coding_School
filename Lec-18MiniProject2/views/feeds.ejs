<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <!-- <link rel="stylesheet" href="/stylesheets/style.css.css"> -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full bg-zinc-900">
  <div class="flex gap-20 text-white px-10 pt-4 pb-10">
    <div class="nav mb-8">
      <a class="text-yellow-100 text-xl hover:text-blue-300" href="/">Back to Home...</a>
      <a class="text-yellow-100 text-xl hover:text-blue-300" href="/profile">Go to Profile...</a>
    </div>
    <h1 class="inline-block text-3xl mb-5 border-b-4 border-white">All Users</h1>
    <div class="posts flex gap-4 flex-wrap">
      <% if (posts.length) { %>
        <% posts.forEach((post) => { %>


          <div class="post mb-2 w-[500px] rounded-xl p-5 border-2 border-zinc-500">
            <h4 class="text-blue-500 mb-3">@ <%= post.user %> </h4>

            
            <p><%= post.content %></p>

            <p class="text-blue-500 font-bold  mt-4 "><%= post.likes.length %> Likes</p>

            <a class="mt-2 text-blue-800 font-bold bg-white hover:bg-yellow-200 px-2 rounded-3xl" href="/like/<%= post._id %>" > <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
            </a>

          </div>
          

        <% }) %>
      <% } else { %>
          <p class="mt-6 text-2xl text-yellow-300 ">No Post Yet....</p>
      <% } %>
       

      
      
    </div>


  </div>
</body>
</html>